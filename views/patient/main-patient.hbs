<html>
<head>
  <title>Paciente</title>
  <style>
    body {
      background-color: #ffffff; /* blanco */
      color: #1f303c; /* azulosc */
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 960px;
      margin: 0 auto;
      padding: 20px;
    }

    h1 {
      color: #4b7b8b; /* azul */
      font-size: 24px;
      margin-top: 0;
    }

    p {
      color: #1f303c; /* azulosc */
      font-size: 16px;
      margin: 0;
    }

    button {
      background-color: #c4f04a; /* amarillo */
      color: #1f303c; /* azulosc */
      border: none;
      padding: 10px 20px;
      margin-right: 10px;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background-color: #4b7b8b; /* azul */
      color: #ffffff; /* blanco */
    }

    .profile-link {
      text-decoration: none;
      color: #1f303c; /* azulosc */
    }

    .profile-link:hover {
      text-decoration: underline;
    }

    .trainers-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: flex-start;
      align-items: center;
    }

    .trainer {
      margin-right: 10px;
      margin-bottom: 10px;
      display: flex;
      align-items: center;
    }

    .trainer-name {
      margin-right: 5px;
    }

    .trainer-image {
      width: 30px;
      height: 30px;
      object-fit: cover;
      border-radius: 50%;
      margin-right: 5px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>¡Hola {{nombre}}!</h1>
    <button onclick="location.href='/perfil'">Perfil</button>
    <button onclick="location.href='/calendario'">Calendario</button>
    <button onclick="location.href='/proximas-clases'">Próximas Clases</button>

    <div class="trainers-container">
      {{#each foundTrainers}}
      <div class="trainer">
        <p class="trainer-name">{{name}}</p>
        <img src="{{image}}" alt="" class="trainer-image">
      </div>
      {{/each}}
    </div>
  </div>
</body>
</html>
